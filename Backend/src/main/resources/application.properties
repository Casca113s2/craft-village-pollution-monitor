spring.jackson.serialization.write-dates-as-timestamps:false
## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

# Set root logger level to DEBUG and its only appender to A1.
# backup: INFO, CONSOLE
log4j.rootLogger=INFO
log4j.logger.org.hibernate=info
# A1 is set to be a ConsoleAppender.
log4j.appender.A1=org.apache.log4j.ConsoleAppender

# A1 uses PatternLayout.
log4j.appender.A1.layout=org.apache.log4j.PatternLayout
log4j.appender.A1.layout.ConversionPattern=%-4r [%t] %-5p %c %x - %m%n
log4j.logger.freemarker.cache = INFO, CONSOLE
logging.file=logs/craftvillage_log.log

server.port = 5000
# Server
# server.port = 80
server.connection-timeout=-1
#----------------------------------
spring.mail.host = smtp.gmail.com
spring.mail.port = 587
spring.mail.properties.mail.smtp.starttls.enable = true
spring.mail.username = cvpmsproject@gmail.com
spring.mail.password = wspeataljtidjefs
spring.mail.properties.mail.smtp.starttls.required = true
spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.connectiontimeout = 5000
spring.mail.properties.mail.smtp.timeout = 5000
spring.mail.properties.mail.smtp.writetimeout = 5000
# =================================
#-----------MYSQL------------
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.datasource.username=root

# -----------LOCAL------------
# spring.datasource.url=jdbc:mysql://localhost:3306/cv_user
# spring.datasource.password=phuchuho


#-----------SERVER------------
spring.datasource.url=jdbc:mysql://db-caps2.catkgfizbysv.ap-southeast-1.rds.amazonaws.com:3306/cv_user
spring.datasource.password=12345678

# ===============================
# JPA / HIBERNATE
# ===============================
#
# spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.current_session_context_class=org.springframework.orm.hibernate5.SpringSessionContext
#----------------------------------
spring.resources.static-locations=classpath:/META-INF/resources/

AI.URL=http://150.95.113.16:8000